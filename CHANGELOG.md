# Updates

* **ICO added to the application**
* **Set the `downloads` folder inside the project folder as the default**
* **Save the selected download folder** using a `.json` configuration file
* **Pressing `ESC` opens a confirmation window** asking whether the user wants to close the application (**YES / NO**)
